# Image exports needed from Figma

Still needed (export from Figma → drop here with these exact names):

| File name            | Used on                        | Suggested export |
|----------------------|--------------------------------|------------------|
| `card-analytics.png` | Work page — card 01 image      | JPG, 2x          |
| `card-ai.png`        | Work page — card 02 image      | JPG, 2x          |
| `card-app.png`       | Work page — card 03 image      | JPG, 2x          |

Optional: `avatar-ben.png`, `avatar-kael.png` (48px referral avatars on the
landing page — they fall back to a gradient if missing).

Already done: `venn.svg` (hero graphic). To update it, just re-export the
graphic from Figma over this file — no code changes needed.

The portrait export is no longer needed (design replaced it with the venn).

How: select the layer in Figma → Export panel (bottom right) → set 2x → Export.
